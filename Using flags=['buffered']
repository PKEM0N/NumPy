# Using flags=['buffered'], and op_dtype=['s']
# Iterate through the array as a string:

import numpy as np

arr=np.array([1,2,3,4,5])

for i in np.nditer(arr, flags=['buffered'], op_dtypes=['S']):
    print(i)

print(i.dtype)
